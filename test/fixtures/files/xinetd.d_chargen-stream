service chargen
{
    disable		= yes
    # disable = no
    id		= chargen-stream
    type		= INTERNAL
    wait		= yes
    socket_type	= stream
    # How protocol is defined by default in CentOS 6:
    # protocol	=  socket type is usually enough
}
